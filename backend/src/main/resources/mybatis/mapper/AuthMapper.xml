<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aloha._24eum.dao.AuthMapper">

  <resultMap id="PersistentLoginMap" type="PersistentLogin">
    <id     property="series"    column="series"/>
    <result property="username"  column="username"/>
    <result property="token"     column="token"/>
    <result property="lastUsed"  column="last_used"/>
  </resultMap>

  <select id="findBySeries" resultMap="PersistentLoginMap">
    SELECT series, username, token, last_used
      FROM persistent_logins
     WHERE series = #{series}
  </select>

  <insert id="insertToken" parameterType="PersistentLogin">
    INSERT INTO persistent_logins (series, username, token, last_used)
    VALUES (#{series}, #{username}, #{token}, #{lastUsed})
  </insert>

  <update id="updateToken">
    UPDATE persistent_logins
       SET token = #{token}, last_used = #{lastUsed}
     WHERE series = #{series}
  </update>

  <delete id="deleteByUsername">
    DELETE FROM persistent_logins WHERE username = #{username}
  </delete>
</mapper>
