<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aloha._24eum.dao.AiSearchLogMapper">

  <resultMap id="AiLogMap" type="AiSearchLog">
    <id     property="id"        column="id"/>
    <result property="userId"    column="user_id"/>
    <result property="query"     column="query"/>
    <result property="response"  column="response"/>
    <result property="createdAt" column="created_at"/>
  </resultMap>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="AiSearchLog">
    INSERT INTO ai_search_logs (user_id, query, response)
    VALUES (#{userId}, #{query}, #{response})
  </insert>

  <select id="findByUser" resultMap="AiLogMap">
    SELECT id, user_id, query, response, created_at
      FROM ai_search_logs
     WHERE user_id = #{userId}
     ORDER BY created_at DESC
     LIMIT 100
  </select>

  <select id="findRecent" resultMap="AiLogMap">
    SELECT id, user_id, query, response, created_at
      FROM ai_search_logs
     ORDER BY created_at DESC
     LIMIT #{limit}
  </select>
</mapper>
